
numpy==1.23.5
pandas==1.5.3
scipy==1.10.0
scikit-learn==1.2.1
google-cloud-bigquery
google-cloud-storage

#our project
altair<5
opencv-python
joblib
#opencv-python-headless
mediapipe==0.10.1
streamlit==1.13.0
watchdog
streamlit-webrtc
gcsfs


# # Trick to install the version of Tensorflow depending on your processor: darwin == Mac, ARM == M1
# tensorflow-macos==2.13.0rc1; sys_platform == 'darwin' and 'ARM' in platform_version # Mac M-chips
# tensorflow==2.13.0rc1; sys_platform == 'darwin' and 'ARM' not in platform_version # Mac Intel chips
# tensorflow==2.13.0rc1; sys_platform != 'darwin' # Windows & Ubuntu Intel chips
# # prevent bq 3+ db-dtypes package error when running training from bq


python-dotenv
